<html>
    <body>
        <h1>Lista de usuarios</h1>
        <ul> 
            <% for (let i = 0; i < users.lenght; i++) { %>
                <li>
                    <%= users[i].name %>
                </li>
            <% } %>
        </ul>
    </body>
</html>